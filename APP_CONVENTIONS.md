# Emma Soft Clay UI — App Conventions (v0.2)

> **Binding rules for EVERY app on this system** — 现有的和以后的(购物、数据库工具、任何)。
> tokens.json 管取值;本文件管"什么角色的 UI 用什么形态"。
> 结构分两层:**第一部分·通用公约**按抽象角色写,和业务无关,永远适用;
> **第二部分·模式库**是具体模式的沉淀,按需增长——新 app 出现新模式时先查这里,
> 没有就按通用公约推导,然后**把推导结果补录进模式库**,保持所有 app 收敛。

---

# 第一部分 · 通用公约(按角色,业务无关)

## A. 导航 (Navigation)

**顶栏**:`[返回] [标题/上下文控件] [动作×≤2]`,无背景无分割线,坐在 appBackground 上。
- **中段绝对居中**:标题/上下文 pill 用绝对定位居中(或左右槽等宽),不随两侧按钮数量偏移。
- **返回/关闭/动作键 = 44px 圆形凸起图标钮**(surface 底 + 1px borderSoft + raisedSoft)。
  禁止裸箭头/裸 icon。整个系统里"可点的独立 icon"只有这一种形态。
- 标题二选一并全 app 一致:居中 title(24/600),或内容区左对齐大标题(display)。
- 中间上下文控件(月份切换、面包屑、搜索入口等)= 44 高 pill 凸起条。
- **底部主导航 = 悬浮 pill tab bar**:radius 24 容器 + raisedMedium;选中 = 凸起白 pill,
  icon 用 Product Main、label 暖黑;未选中无底 + tertiary。2–5 个 tab。
- 层级导航(进入详情/子页)一律推入新页 + 返回键,不用横向 tab 套 tab。

## B. 选择 (Selection) — 互斥选一

**任何"N 选 1"控件只有一种形态:凹槽底座 + 凸起白选中项**(radius 18/14)。
适用于:视图切换、周期切换、筛选档位、表单单选、商品规格选择、排序方式……
- 禁止实心 Main 色 pill 表示选中;禁止无底座的文字变灰。
- 选项 >5 个或动态数量 → 用凹槽容器包裹的可横滚 chips,选中仍是凸起白。
- **多选** chips:未选 = Tint 底 + Ink 字;选中 = Main 底白字(小面积允许)。

## C. 集合 (Collections) — 列表/网格/表格

- **列表行**:`[44 图标底座] [标题+副标题] [右值/状态 + chevron ›]`,行高 ≥64,
  多行共用一张卡、行间 divider hairline;不要一行一卡堆叠。
- **图标底座统一:44×44、radius 12、Hue Main 实心底 + 2px 白描边 icon**。
  禁止圆形底、禁止底里放文字/字母/数字。头像是唯一允许的圆形(因为是图片)。
- **网格卡**(商品、相册这类):surface 底 radius 16–20 + raisedSoft,图占上、文字区在下;
  价格/关键值用 bodyStrong,次要信息 caption tertiary。
- **数据表格**:表头 caption tertiary + divider 分隔;行 hover = surfaceWarm;
  选中行 = 凹陷槽(sunken);数字右对齐用 tabular-nums。

## D. 录入 (Data Entry)

- 输入框/搜索框/文本域 = 凹陷(surfaceSunken + sunken),focus = accent 1px 边线,无发光。
- 步进器、数量选择 = 凹槽底座 + 两侧 32px 凸起圆钮。
- 开关 = 凹槽轨道 + 凸起白圆钮;开 = 轨道变 Product Main。
- 提交主按钮 h48 radius 14(暖黑或 accent 底);一屏一个主按钮。

## E. 状态与反馈 (Status & Feedback)

- 语义色只用固定状态色(success/warning/danger/info),Tint 底 + Ink 字的小徽章,
  不占用 Product/辅助色名额。涨跌、库存、同步状态、校验错误都算这类。
- 空状态 = 凹槽容器 + 18px 描边 icon + tertiary 文案;不用插画不用 emoji。
- 进度(线性/圆环)遵循 DESIGN_SYSTEM.md §7:凹陷轨道 + Main 填充。
- Toast = 浮起小卡(floating),顶部滑入;不全屏遮罩。

## F. 浮层 (Overlays)

- 移动端一律**底部 sheet**:radius 28 顶圆角、floating、36×4 把手;主按钮固定底部。
- 桌面端居中对话框:radius 24 + floating。遮罩 = rgba(46,42,40,.35)。
- 确认破坏性操作:danger 状态色只出现在确认按钮上。

## G0. Hero 卡底色恒定(同组件不换色)

- 同一个 Hero 卡在不同视图/状态下**底色必须恒定**,用 Product 主色的淡 tint,
  **不随语义在红/绿/靛之间翻**(那会让同一组件看起来像三个东西)。
- 语义(收入绿/支出红/涨跌)只放在**卡内的大数字或小徽章**上,不铺到卡底。
- 大面积语义色也要避免:单指标视图(如"支出"页)标题已表意,大数字直接用
  Product 主色即可,不必整屏红/绿。红/绿留给需要并列区分的小元素
  (如收支页底部的收入/支出小格:小圆点 + 小号数字)。

## G. 每屏彩色预算(全局硬规则)

`1 Product 主色系 + ≤1 辅助色系 + 状态色`。高饱和 Main 只在:图标底座、进度、
圆点、选中小元素、主按钮。大面积只有 Tint。每个 app 立项时先认领 Product Color。

## H. FAB 与渐变禁令

- FAB = **56px 圆、Product Main 实心或暖黑底、白色 2px 描边 icon、floating 阴影**,右下角距边 20。
- **全系统禁止渐变填充**(按钮、FAB、背景、图表)。唯一例外:圆环超额段的同色系渐深。

## I. 图形符号

- 界面 chrome(按钮、卡片标题、空状态、tab)**禁止 emoji**,一律 2px 描边 icon。
  emoji 只允许出现在用户自己输入的内容里。

## J. 图表

- 线/柱/环只用 Product Main;对比系列才允许加一个辅助色系 Main。图表底用 Tint 或透明。
- **图表空态 = 卡内凹陷槽 + 18px 描边 icon + tertiary 文案**,禁止大白卡里孤零零一行灰字。
- 大型圆环仪表同样遵循 DESIGN_SYSTEM §7 ring 规则:凹槽轨道 + Main 填充,不做实心大饼。

## K. 选择控件布局

- **同一行只放一组选择控件**。两个维度(如 时间×指标)分两行,或把时间维度收进顶栏 pill 上下文控件。
- 选项 >5 或一行放不下 → 凹槽容器内横向滚动,选中仍是凸起白;"筛选"这类动作不混进选项里,独立为 44px 圆钮。

## L. 多模块 app 的色彩例外

- 模块色(睡眠蓝/训练绿/营养琥珀…)只出现在 **≤44px 小件**:icon 底座、圆点、图例 chip。
- 同屏的大面积、正文、主按钮仍守 G 的 1+1+状态 预算。模块 chip = Tint 底 + Ink 字。

## M. 状态栏

- 浅底上状态栏文字恒为 textPrimary。禁止白字压浅底。

---

# 第二部分 · 模式库(具体模式,按需增长)

> 新模式出现 → 先查这里 → 没有则按第一部分推导 → **推导结果补录到这里**。

## P1. 日历(健康 app 沉淀)
选中日 = 凹陷槽 radius 12;今日 = accent 1px 描边圈;事件 = 下方 5px Main 圆点 ≤3;
图例 chips = Tint 底 Ink 字,范围类用 1.5px 虚线描边。

## P2. 金额与涨跌(记账 app 沉淀)
金额 bodyStrong / 大数字 display + tabular-nums;涨跌用 success/danger 状态色
(▲▼ + 数值),不引入新色相;货币符号同字号 60% 大小。

## P3. 指标详情入口(健康 app 沉淀)
单个数据指标的趋势/详情不放顶栏、不在页内展开:**入口就在数据行本身**
(行尾 Main 色趋势小 icon + chevron),点开底部 sheet,图表画在凹陷槽里(Main 色线)。
顶栏动作键只放全局功能(设置、全局统计)。

## P4. 购物(角色扮演购物 app 沉淀)
- 商城 = 用户的**真实商品库**(手动填名称+价格+备注),不是虚拟商店;空态引导"记下你真会买的东西送给 TA"。
- **一 app 多业务线时按线认领子色**:网购=Teal(冷,配实物)、外卖=Amber(暖,配食物),互补冷暖平衡。
  规则:app 级元素(tab、通用按钮、"我的")用主色 Teal;**进入某条线后,该线的卡片/图标座/chips/订单状态/详情全切该线色**
  (外卖页=Amber 主+Teal 辅,网购页=Teal 主)。每屏仍守 G 的 1 主+1 辅+状态 预算。
- 商品字段:名称、价格、**店铺/品牌(可选)**、备注、类型、配图(可选)。品牌只是小字元数据
  (列表卡 11px tertiary 小字、行副标题前缀、订单/聊天 prompt 带上),**不做店铺层级页**—避免空店铺尴尬。
- 商品备注(三分糖去冰)贯穿 加购→购物车→订单详情;订单留言做成**对话气泡**(用户右侧 Main 气泡,角色回应左侧灰气泡)。
- 送达:网购按日期、外卖按时长,存**绝对目标时间戳**而非剩余时长(PWA 关开重进仍准);详情页用**配送时间线**,不用倒计时环。
- **用户自建商品的图标(核心难点)三层兜底,永不现画图**:①用户上传照片(配图字段);
  ②没照片→按分类套预置线性图标库(奶茶/咖啡/蛋糕/甜点/花束/玩偶/首饰/数码/香水/零食… ~15-20 个,分类同时当筛选维度);
  ③兜底→品牌/商品名首字 monogram(暖黑字 + Tint 底座,如 Tiffany→「T」、霸王茶姬→「霸」)。
- **外卖列表按店铺分组**:一店一卡,卡头 = 店铺 monogram 座 + 店名 + 送达时长,卡内 1–2 条商品行(名+备注+价+加购)。
  网购列表 = 一行两个网格卡。商城保留少量初始项 + 用户存过的真实商品,网购/外卖各一页。

## P5. (预留)数据库/表格工具 → 按 C 数据表格 + B 筛选 chips 推导

---

## 交付自检(每页)

- [ ] 可点独立 icon 全部是 44px 圆形凸起钮
- [ ] 所有单选控件 = 凹槽+凸起白,无实心色 pill
- [ ] 图标底座全部圆角方 12、Main 底白描边 icon
- [ ] ≤1 主 + 1 辅 + 状态色;大面积只有 Tint
- [ ] 至少一处凹陷;空状态用凹槽
- [ ] 无渐变填充、无 emoji 图标;FAB 为实心 Main 圆
- [ ] 一行只有一组选择控件;图表空态用凹陷槽
- [ ] 新模式已补录进模式库

## P6. 聊天设置分组页（2026-09-12 Emma 已确认）

- 44px 返回圆钮、16px/600 居中标题，顶栏固定在滚动区外，安全区使用 `var(--chrome-top)`。
- 紧凑身份行使用 48px 头像、显示名和原名两行，右箭头进入角色设定；不再附加路径说明。
- 纯设置目录可省略左侧图标底座，用文字和右箭头，行高至少 64px；同组共用 surface 卡与 divider。查找入口单独一组。
- 备注编辑沿用凹陷输入与底部主按钮；底栏只放保留的常用操作。
- Emma 指定例外（2026-09-12）：Chat 顶部设置入口仅显示三个点，不显示圆形底、边框或阴影；保留 44px 点击范围。
- 当前发送/生成交互与独立设置页为固定前端：输入框聚焦时发文字，失焦时触发回复；不再提供“发送按钮代替生成按钮”开关，旧存档关闭值也按固定交互读取。

## P7. 库存紧凑列表（2026-09-13）

- 多条食材共用一张 surface 卡，72px 起的行高、divider 分隔；行内显示名称、简短余量、位置与展开箭头。
- 默认收起操作，一次只展开一行；展开区使用 Product Tint，展示完整规格和原有操作，长名称允许换行。
- 名称搜索与位置筛选分两行；筛选遵循凹槽底座与白色凸起选中项。无匹配时显示空状态，保留筛选入口。
- 消耗或核对不改变列表顺序；零库存退出列表，撤销恢复原位置。

## P8. 库存详情与编辑（2026-09-13）

- 从展开行进入独立详情页，顶栏保持 44px 返回圆钮与居中标题；返回后保留原列表和展开位置。
- 编辑内容分为“基本资料”“收纳与日期”，使用带字段标签的凹陷输入/原生 select；一屏只放一个“保存修改”主按钮。
- 当前库存另列为只读凹陷说明，数量变动仍在列表操作区完成，避免修改规格或位置时误改余量。
- 单条历史共用一张 surface 列表卡，以 64px 行和 divider 呈现事件、时间、备注与变化量；已撤销事件降低对比度。

## P9. 库存空间视图（2026-09-13）

- 3D 场景作为库存页的可选视图，按需加载；外层使用 Product Tint，视图切换沿用凹槽与白色选中项。模型保留素材本身配色，按钮与文字只用 Clay tokens。
- 自动摆放每组最多 9 条库存，超出翻组；一个模型代表一个批次，编号对应下方名称按钮，44px 点击区域同时提供文字入口。
- 冰箱不使用分区标签，直接点击上下门独立开合；关门时隐藏对应食材编号。普通食材模型冷藏层板每组 9 条、冷冻 3 条、门内上中下三层各 2 条；12 格蛋盒占整行或整层门架，超出翻组。四个摆放按钮用两列布局。加载失败保留列表；减少动态效果时，门与抽屉直接到最终位置。各门保留键盘/读屏等价按钮，透明抽屉保留文字开合入口。
- 点模型或名称先选中食材，在凹陷面板中查看详情或选择摆放位置；移动按钮与当前不可重复选择的位置清晰区分，保存期间禁止重复操作。门上编号跟随模型投影，不固定在画布上。
- 详情新增“这条库存已用完”次按钮，复用原账本清零与撤销；资料保存不修改数量。批次只保存语义摆放位置，不保存模型坐标或外形。
