# 2026-09-12 上游同步验收记录

状态：执行中。此文记录本次实际授权与验证；不表示 main 已更新。

续接材料已保存到 `docs/upstream-sync-pending/`，含原始产品清单、语音适配计划、独立异格计划和未应用的 GitHub 备份补丁。原清单中的推荐不覆盖下方用户已批准范围。

## 已批准范围

Emma 已在产品调研后明确选择：

- 调研清单第 1 节（回复操作与聊天）、第 2 节（见面/剧情/记忆/彼方书库）、第 3 节（API/消息补收/备份/工具）全部采用，包括新版范围清理。保留所有 EM 契约。
- 语音收藏改为文字语音可收藏；收藏动作不请求 TTS、不要求语音 API。若已有真实音频，可连同文字保存；没有音频时不补生成。
- 第 4 节仅采用通话偏好、页面恢复；ElevenLabs、MiniMax 新参数、Live2D/衣柜/触摸演出、日记换肤、PNG/系统分享扩展、Spark/人格模拟/小红书主页等其余功能暂缓。第 3 节所需共用依赖仍按具体调用保留。
- 异格从彼方/活动室提取为剧情中的独立入口；直接选择人格分岔和世界背景，提供随机按钮。用户已在后续选择中确认这一方案。取消货币与芯片抽取依赖；不接 NPC、模块商店、钓鱼、布告板、恐龙箱庭。
- 协同工作暂缓。上游统计、作者服务配置、周年换装、Android 发行和部署绑定不引入。

本次授权包括所选功能的适配与验收分支上的提交；不包括推进或推送 main、部署线上服务、执行实际数据清理或写入外部工具。新的产品取舍若超出以上范围，带回具体问题。

## 锁定版本与基线

- fork：`b61078f68e8c1f8d836480ac6539767ff8ebca1d`。
- upstream：`ac48e7c6fca99c4524dc968bd56805b0ec127b4a`。
- 共同基线：`51b73963e3b8d82eee66f3a60e7cd40e0783aaf6`。
- 开始前已从 origin/main 快进拉取，返回已是最新。
- 当前验收分支：`merge-upstream-2026-09-12`，main 保持上述 fork 提交。
- EM 参考备份位于本机 `_em_backup/2026-09-12-b61078f6/`，包含相对共同基线改动的 713 个现存文件；仅本地参考，不提交备份副本。
- 旧版本 Vitest：4094 通过、5 跳过、0 失败。
- 旧版本 Vite 构建通过；这是前端构建，不是完整 Worker 构建或部署。
- 旧版本 tsc 有 47 条诊断，保存为基线；不顺带修上游纯类型问题。
- 现有 EM 哨兵：83/83 锚点通过；不等于 83 项真机验证。

## 写入检查与行为合同

主工作流：code-quality-workflow。A0 路由 `code-quality-workflow`，知识快照 `2026-08-18`，已读 `ST-A0`；路由返回的设计候选未采用。

目标是按批准范围更新可运行的验收分支。红线是保留 EM 行为/数据/角色 API/隐私边界、保持 main 和线上服务不变、不带入未选玩法。验收以定向测试、完整单测和构建、备份副本往返、浏览器交互为依据；人工体验单列。

结构决定为 No Refactor：分阶段移植上游功能，不顺带改架构。单独局部修复为异格持久化错误可见、结果可恢复，以及文字语音收藏不生成音频。大同步不按普通 80 行修补伪装；每组记录真实变更范围并验证。

界面依赖已补齐：Emma 提供根目录 `APP_CONVENTIONS.md` v0.2，已完整读取，同时读取 `design-system/DESIGN_SYSTEM.md` 和 `utils/clayTokens.ts`。该版本没有单独 §0、通用标题写为 24px；本仓库 `.claude/CLAUDE.md` 的具体顶栏要求为 16px/600、44px 圆钮、chrome-top，按这一具体规则实现，其余按 v0.2 公约。已向 Emma 说明版本差异，不再等待缺失文件答复。

## 必须保住的主链

角色有效 API 与 secondary 优先级、Offline 延迟到达、Notion 快捷日记和额外列、文字语音/主题、收藏 owners 与图片身份、EPUB/批注、Token/最终召回简报、Finance/Health/Shopping/Map/SmartHome 备份、Apple schema 副本归一化和 HA 凭据剥离、位置仅本地粗略输出、七夕稳定键与幂等、已有 agenda/头像兼容、品牌和页面规范。

## 后续验证

已接入上游 `3da0c1e3` 的状态时钟修复：设备时间回拨后状态版本继续递增；收到明确陈旧状态拒绝时读回远端水位，最多对齐重发一次。保留有效角色 API 与现有请求打包。相关时钟、客户端、角色 API 和凭据同步测试 168/168 通过，暂未对线上 Worker 操作。

### 当前检查点已实现

- MCP 协议协商、诊断元数据、破坏性工具前台确认/后台排除、input_required 停止；前台和 Worker 传递协商后的协议。保留 Apple schema 副本归一化、HA 路由与凭据剥离。前台相关 95 项、Worker 233 项定向测试通过。
- 普通 chat/completions 不自动重发；请求前处理 Claude 采样参数、流式组装保留响应状态、网络失败诊断保留真实请求信息。保留 EM Vertex/Gemini 响应归一化和 API 日志去重。`claudeProxyCompat` 工具及其测试已移植，但业务调用尚未接线，不能记为该兼容路径完成。
- 文字语音核心：没有音频也可存；已有音频与索引同事务提交；重复收藏不删除已有音频；取消不碰来源缓存或普通收藏；损坏索引/读写失败明确报错。快照保留说话者、段落和显式字幕，不把块外闲聊当翻译；只读取已有音频，无合成/翻译/播放操作。
- 文字语音备份：完整/媒体 ZIP 外置音频、导入前验证附件；纯文字包通过独立 `voiceFavoritesIndex` 字段携带文字并标明省略音频，导入只写该索引，不清整个 assets。老版本导入器忽略新字段，也不会因只有一条素材记录误清旧素材。

### 尚未接入或尚未验证

GitHub 核心放在待应用补丁中，须与新版设置界面一起接入。额外修复了“发布成功但回执丢失时误删完成备份”；五种发布/回查场景及其他测试共 20 项通过，仅模拟，没有上传真实备份。

语音收藏的各页面入口、聊天其余可靠性与折叠设置、剧情/记忆/书库其余改进、消息补收其余接线、通话偏好、页面恢复、独立异格均仍待完成。独立异格当前只有已确定的方案，没有创建故事存档或请求模型。

独立审查已检查本检查点的 MCP/网络核心，修复了缺失诊断函数 import。语音核心的只读审查又复现并修正三项：显式 null 索引不能当空库覆盖、非法音频 marker 不能静默跳过、空语音标签不能落成收藏正文。修正后的语音定向测试 21/21 通过；并发写入与真实普通收藏 owners 保留的独立探针通过。

本检查点完整 Vitest：4148 通过、5 跳过、0 失败。随后仅对上述三项边界做修正，重跑受影响的 21 项全部通过。前端和全部 6 个 Worker 构建通过；EM 锚点 83/83 通过；tsc 仍为原有 47 条诊断，无本次新增错误（原始联合类型报错的枚举顺序可能不同）。GitHub 待应用补丁不计入这些当前运行代码检查，单独记录其 20 项模拟测试。

此前核心检查点没有浏览器验收。没有手机验收、真实模型/外部 MCP 调用、线上 Worker 更新或真实备份上传，不能标为人工接受。

先核对具体冲突与排除范围，再逐组接入输入/剧情、API/数据/消息、记忆/书库、通话/恢复与独立异格。每组先跑相关测试；最后完整 Vitest、EM 哨兵、前端与所需 Worker 构建，并比较类型基线。使用数据副本检查备份、收藏/剧情/异格；实际模型、手机和外部工具未经运行不得写成通过。

### 输入操作检查点（2026-09-12）

- 私聊/群聊接入四个共用设备设置：底部生成按钮、回车发送、发送后自动回复、表情联想；默认保持原操作，设置进入完整/纯文字备份，当前设备两种会话实时共用。
- 输入时按钮发送文字，结束编辑后按钮生成；草稿保留，输入法选字和 Shift+Enter 不误发；表情候选使用所有可见分类且不清文字草稿。
- 自动回复在实际消息保存/图片处理完成后等待 2 秒，编辑/面板打开时暂停；取消、切会话、离开、其他生成接手后，晚到的发送结果不能重启倒计时。EM Offline 仍走原延迟回复，Notion 手动请求开始准备时取消旧倒计时，角色 API 预设面板也暂停倒计时。群聊保留停止按钮和导演/轮流模式，保存期间的新草稿不清除。
- 新设置使用 Clay 凸起卡、凹槽开关与说明、44px 说明圆钮。旧聊天主题、个人快捷工具栏、角色 API 与文字语音入口保留。
- 19 项输入/偏好/联想定向测试通过；完整 Vitest 4167 通过、5 跳过，前端 + 6 Worker 构建通过，83/83 EM 锚点通过。独立审查发现并修复群聊晚到保存清新草稿、API 预设面板未阻止倒计时、Notion 准备期间倒计时抢先请求三个边界。
- 本地浏览器使用全新 localhost 数据：看到并保存新设置、显示展开说明；关闭回车发送后输入两行草稿，结束编辑后切成生成按钮，草稿原样保留，顶部闪电隐藏。未触发模型请求。群聊实际页面、手机键盘与备份 UI 往返尚未实测。
