# 厨房 3D 第一版

## 已实现的范围

库存页的「看看冰箱」按需加载 Three.js。当前模型为原创圆角双门冰箱，上层冷冻、下层冷藏、底部两个透明抽屉。分区切换分别展示真实库存，模型或编号点击进入现有详情页；详情清零复用原 `FINISH` 操作，返回库存页可撤销。不导入演示食物，不另存数量。

第一版一个模型代表一条批次，不按鸡蛋个数复制，也不按液体余量变形。牛奶/果汁暂用纸盒，鸡蛋与肉使用对应 Kenney 模型，其他食物用袋子。名称匹配只是外观提示；它不改变食材类型或库存。

## 文件边界

- `utils/kitchenDb.ts`：唯一库存事实来源，本轮不改存储结构。
- `utils/kitchenSceneLayout.ts`：只读投影。仅 quantity > 0 且 storageZone 匹配所选分区的批次进入场景，按创建时间与 id 排序；冷藏每组 9 条，冷冻每组 3 条。最后一页消失时钳制到有效页；清零/撤销、位置编辑自然反映在下一次投影中。
- `components/kitchen/KitchenFridgeScene.tsx`：模型加载、定向相机、门动画、模型命中检测、文字替代入口。加载失败仍能从列表处理库存。
- `scripts/build-kitchen-model.mjs`：原创参数化建模，输出 `public/kitchen/models/fridge.glb`。不用依赖 Blender 安装；包含上下门轴、透明空心抽屉、玻璃搁板、内胆筋条、风口、灯罩、橡胶门封与金属把手。烤漆颗粒和拉丝法线 PNG 嵌入 GLB，可作为普通 glTF 导入 Blender；尚未在 Blender 重新验收。门轴为 `DoorPivot` / `FreezerDoorPivot`；抽屉为 `CrisperLeft` / `CrisperRight`。模型约 2.3 MB，81 个网格，未做最终移动端压缩。
- `public/kitchen/`：随站点发布的模型与版权说明。食材 GLB 仍引用同目录的 `Textures/colormap.png`，不能漏掉。

## 运行与性能边界

不新增依赖，复用项目现有 Three.js；场景懒加载，像素倍率上限 1.5，只在加载、尺寸变化与门/抽屉动画时绘制，离屏/后台停止绘制。使用 RoomEnvironment 生成环境反射，使玻璃和金属可辨。退出时释放画布、几何、材质、贴图、环境图、观察器和事件；迟到的加载结果也释放。减少动态效果时直接到最终位置。关门先收回抽屉。模型不参与库存备份，换模型不迁移数据。

每组 9 个模型配 44px 编号点击区与名称按钮，小屏幕长名称省略，进入详情看全名。相机固定，不提供拖拽、缩放或物理碰撞。

## 验证与下一步

49 项厨房定向测试通过：20 条库存分页、零库存/其他位置排除、少量余量、缺失贴图文件检查、门轴与隔板契约，以及场景入口 → 详情清零 → 返回后移除 → 撤销恢复。生产构建通过，全仓类型检查仍有既存问题，厨房文件无错误。EM 哨兵 82/83，既存聊天搜索入口缺失。

390×844 本地浏览器确认带贴图的冰箱与食材、按钮开门、点击鸡蛋模型进入真实详情；没有修改用户库存。正式 iPhone/PWA、断网/上下文丢失及长时间性能未实机验收。清零与撤销链路在隔离自动测试验证，未在用户库存中试扣。

2026-09-13 模型重做：50 项厨房定向测试通过，增加冷冻区分页/高度检查、双门/抽屉节点与透明材质/内嵌贴图契约。浏览器确认新的双门圆角外观、内胆结构、透明抽屉拉出，无模型加载错误。390px 预览还发现并修复了切换空分区时旧编号读取新数组的错误：编号改按 lotId 对应当前库存；复测冷藏 → 空冷冻区、抽屉状态保留和关门收回正常。正式 iPhone 性能与 Blender 导入待验收。

下一步先由 Emma 体验新外观，再做旁边有门的 pantry。当前上下门一起开合，两只抽屉一起拉出；抽屉还不单独分配蔬果，切换分区只显示该分区食材。待收纳场景、自由摆放、余量外形、菜谱、角色查询仍未实现。
