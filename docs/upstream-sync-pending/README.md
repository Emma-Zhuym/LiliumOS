# 上游同步待续接材料

这是 `merge-upstream-2026-09-12` 的分阶段检查点，**并非整批同步完成**。实际授权见 [验收记录](../upstream-sync-2026-09-12.md)。main 仍保持 `b61078f6`。

- `upstream-sync-review.md` 是执行前清单，保存第 1–4 节的完整含义；其中旧推荐已被用户后续选择覆盖，不能重新当作待批准范围。
- `voice-favorites-plan.md` 是文字语音适配方案。核心存储、快照、备份及聊天/通话/见面/收藏界面已接入；浏览器验证装饰语音，完整通话/见面与真机边界见验收记录。
- `standalone-plan.md` 是剧情内独立异格方案。用户已选直接组合 + 本地随机；代码尚未实现。
- `github-backup.patch` 是 GitHub 备份核心 + 类型的历史移植材料，**已应用，不要重复应用**。当前源码还包含后续设置接线和提示路径修正；以运行源码和验收记录为准。

GitHub 的代理明确选择、备份状态/缺片展示已与核心一起接入。开关和传输共用 `githubUseProxy=true && githubProxyConsentVersion=1`，默认直连。三个上游测试使用数字 `lastModified`，留下已知纯类型诊断：本组后 tsc 为原 47 + 3 条，运行测试通过；无新增生产代码诊断。

下一次续接先确认当前分支、未提交修改和远端状态并 pull；读取验收记录与此目录。Emma 已提供并已完整读取根目录 `APP_CONVENTIONS.md` v0.2；其没有 §0，顶栏依 `.claude/CLAUDE.md` 的 16px/600 具体规则，其余按公约，不再等待文件答复。四个聊天输入设置、文字语音与 GitHub 备份已接入并验证，余项继续按原批准范围执行。推进/push main 仍需独立明确批准。
