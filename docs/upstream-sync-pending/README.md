# 上游同步待续接材料

这是 `merge-upstream-2026-09-12` 的分阶段检查点，**并非整批同步完成**。实际授权见 [验收记录](../upstream-sync-2026-09-12.md)。main 仍保持 `b61078f6`。

- `upstream-sync-review.md` 是执行前清单，保存第 1–4 节的完整含义；其中旧推荐已被用户后续选择覆盖，不能重新当作待批准范围。
- `voice-favorites-plan.md` 是文字语音适配方案。核心存储、快照与备份已实现；聊天、通话、见面和收藏界面仍待接入。
- `standalone-plan.md` 是剧情内独立异格方案。用户已选直接组合 + 本地随机；代码尚未实现。
- `github-backup.patch` 是已准备并测试过的 GitHub 备份核心 + 类型补丁，**没有应用到当前运行代码**。包含上游 32 MB 分片/完成清单/直连默认，以及本次补的发布回执丢失时回查而不误删备份。其 20 项定向测试已通过；应用检查通过。

GitHub 补丁必须与 `apps/Settings.tsx` 中的代理明确选择、备份状态/缺片展示一起接入。旧 UI 按 `githubUseProxy` 显示，新核心还要求 `githubProxyConsentVersion=1`，直接启用半套会出现开关显示与实际线路不一致。补丁中的三个上游测试使用数字 `lastModified`，有已知纯类型诊断；不把它算作当前运行代码新增诊断。

下一次续接先确认当前分支、未提交修改和远端状态并 pull；读取验收记录与此目录。缺失的 `APP_CONVENTIONS.md §0` 替代规范仍等用户答复，不能把存档计划当作已经收到答复。获准后把 GitHub 补丁与 UI 接入作为同一批处理，重新验证并逐项完成全部所选功能。推进/push main 仍需独立明确批准。
